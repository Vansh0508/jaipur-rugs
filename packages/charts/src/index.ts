// Bklit chart configs live here (AGENTS.md Section 2 / 4).
// Every app builds charts through this package rather than configuring Bklit ad hoc,
// so chart styling/behavior stays consistent across department apps.
//
// Stub — no chart configs implemented yet. Populate once the first app needs a chart.

export const CHARTS_PLACEHOLDER = true;
